Nel progetto abbiamo un database che viene creato ogni volta che il programma viene avviato,
sono stati creati due tipi diversi di scheduler, uno tipo che viene eseguito all'avvio dell'applicazione il secondo
viene gestito tramite l'esposizione di end-point, per ora possiamo:
1. Effettuare le CRUD sul job
2.  Richiamare uno job tramite il controller Scheduler per poter eseguire/fermare o ottenere uno status sullo o sugli scheduler attivi.

job configuration
creare un job senza salvarlo nel database nello scheduler
catturare la risposta e salvarla nel database


Un controller che espone un api al quale gli viene passato un oggetto json,
che gli applica un controllo e lo passa al service.
il service estrae le informazioni e crea uno scheduler senza salvarlo nel database ma
che viene registrato nella memoria e quindi c'è poi la possibilità di tenerne traccia.


Rest db
TRUNCATE TABLE JOB_CONFIGURATIONS;
ALTER TABLE JOB_CONFIGURATIONS ALTER COLUMN ID RESTART WITH 1;